# CI/test-only dependencies -- deliberately separate from requirements.txt.
# The unit tests only import the pure, CPU-only helpers in each script
# (calibrate(), majority_filter(), s3_to_https()), so this skips torch and
# its bundled CUDA runtime entirely -- the single biggest chunk of the
# ~10GB production image, and unnecessary for what CI actually exercises.
mlflow==3.15.1
numpy==2.5.1
pystac-client==0.9.0
pytest==8.3.4
rasterio==1.5.0
ray[default]==2.56.1
ruff==0.9.2
yamllint==1.35.1
